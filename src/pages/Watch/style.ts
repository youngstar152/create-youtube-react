// src/pages/Watch/style.ts

import { makeStyles } from "@material-ui/core";

export default makeStyles({
  root: {
    maxWidth: 1400,
  },
  // CardのPaddingを指定する
  cardPadding: {
    marginBottom: 10,
  },
});
